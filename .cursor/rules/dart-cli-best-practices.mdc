---
alwaysApply: true
---

# 🎯 Melhores Práticas para Criação de Tasks com Dart CLI

## **Comandos Dart CLI Disponíveis:**
- `dart task-create` - Cria nova tarefa
- `dart task-update` - Atualiza tarefa existente
- `dart task-delete` - Remove tarefa
- `dart task-begin` - Inicia trabalho em uma tarefa

## **Sintaxe Correta para Criação de Tasks:**

```bash
dart task-create "Título da Tarefa" -d "Dartboard" -p 3 -s "Status" -t "tag1" "tag2"
```

### **Parâmetros obrigatórios:**
- `title` (posicional): Título da tarefa

### **Parâmetros opcionais:**
- `-d, --dartboard`: Dartboard (valores válidos: `FalaChefe/Tasks`, `General/Tasks`, `Personal/Tutorial tasks`)
- `-p, --priority`: Prioridade 0-3 (0=baixa, 3=alta)
- `-s, --status`: Status (valores válidos: `To-do`, `Doing`, `Done`)
- `-t, --tag`: Tags (múltiplas permitidas)
- `-a, --assignee`: Emails dos responsáveis
- `-i, --size`: Tamanho/Esforço (1,2,3,5,8)
- `-r, --duedate`: Data de vencimento (formato ISO)

## **Sintaxe Correta para Atualização de Tasks:**

```bash
dart task-update TASK_ID -s "Done" -e "Novo Título"
```

### **Parâmetros:**
- `TASK_ID` (posicional): ID da tarefa (12 caracteres)
- `-e, --title`: Novo título
- `-s, --status`: Novo status
- `-d, --dartboard`: Novo dartboard
- `-p, --priority`: Nova prioridade
- `-t, --tag`: Novas tags
- `-a, --assignee`: Novos responsáveis
- `-r, --duedate`: Nova data de vencimento

## **🔴 Regras Obrigatórias para Criação de Tasks:**

### 1. **Títulos Descritivos**: 
Sempre incluir contexto específico no título
- ❌ "Implementar funcionalidade"
- ✅ "T8: Sistema de classificação multi-camada - Intenção/Sub-intenção/Urgência/Contexto"

### 2. **Dartboard Correto**: 
Sempre usar `FalaChefe/Tasks` para tarefas do projeto

### 3. **Prioridades Padronizadas**:
- `3`: Crítico/Alto (milestones principais)
- `2`: Médio (sub-tarefas importantes)
- `1`: Baixo (melhorias, documentação)
- `0`: Muito baixo (tarefas opcionais)

### 4. **Status Padronizados**:
- `To-do`: Tarefa criada, não iniciada
- `Doing`: Tarefa em progresso
- `Done`: Tarefa concluída

### 5. **Tags Contextuais**: 
Sempre incluir tags relevantes
- Tags de milestone: `M1`, `M2`, `M3`, `M4`
- Tags de tipo: `Classificação`, `Integração`, `Testes`, `Documentação`
- Tags de componente: `Agent Squad`, `WhatsApp`, `Supabase`

## **🔄 Atualização de Progresso Obrigatória:**

### **Ao concluir uma tarefa, SEMPRE:**

1. **Atualizar status para `Done`:**
   ```bash
   dart task-update TASK_ID -s "Done"
   ```

2. **Atualizar título com realizações:**
   ```bash
   dart task-update TASK_ID -e "Título ✅ CONCLUÍDO - Detalhes do que foi realizado"
   ```

3. **Atualizar TODO list local** com status `completed`

4. **Atualizar Memory Bank** (`progress.md` e `activeContext.md`) com:
   - Status da tarefa
   - Detalhes das realizações
   - IDs das tarefas criadas
   - Próximos passos

## **📋 Exemplos Práticos:**

### **Criar task de milestone:**
```bash
dart task-create "T8: Sistema de classificação multi-camada - Intenção/Sub-intenção/Urgência/Contexto para Agent Squad" -d "FalaChefe/Tasks" -p 3 -s "To-do" -t "M2" "Classificação" "Multi-camada" "Intenção"
```

### **Atualizar task concluída:**
```bash
dart task-update bzNxhHEGtwH6 -e "Milestone M1 - Fundação do Agent Squad ✅ CONCLUÍDO" -s "Done"
```

### **Criar task de sub-tarefa:**
```bash
dart task-create "T9: Perfil e memória persistente por agente - Leo(Financeiro), Max(Marketing), Lia(RH)" -d "FalaChefe/Tasks" -p 3 -s "To-do" -t "M2" "Perfil" "Memória" "Agentes"
```

## **✅ Validação de Valores Válidos:**

Antes de criar tasks, sempre verificar valores válidos:
- **Dartboards**: `FalaChefe/Tasks`, `General/Tasks`, `Personal/Tutorial tasks`
- **Status**: `To-do`, `Doing`, `Done`
- **Prioridades**: `0`, `1`, `2`, `3`
- **Tamanhos**: `1`, `2`, `3`, `5`, `8`

## **🚨 Erros Comuns a Evitar:**

1. **Não usar descrições nos títulos**: O Dart CLI não tem campo de descrição separado
2. **Esquecer de atualizar status**: Sempre marcar como `Done` quando concluir
3. **Usar dartboard incorreto**: Sempre usar `FalaChefe/Tasks` para o projeto
4. **Não documentar realizações**: Sempre incluir o que foi feito no título atualizado
5. **Esquecer de atualizar Memory Bank**: Documentar progresso nos arquivos locais

## **🎯 Checklist para Criação de Tasks:**

- [ ] Título descritivo e específico
- [ ] Dartboard correto (`FalaChefe/Tasks`)
- [ ] Prioridade adequada (0-3)
- [ ] Status inicial (`To-do`)
- [ ] Tags relevantes incluídas
- [ ] Comando executado com sucesso
- [ ] ID da task registrado
- [ ] Memory Bank atualizado
- [ ] TODO list local atualizado