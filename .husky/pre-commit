#!/bin/sh
# Pre-commit hook para validação multi-camada

echo "🎭 BMad Orchestrator - Validação Multi-Camada"
echo "=============================================="

# Cores para output
RED='\033[0;31m'
GREEN='\033[0;32m'
YELLOW='\033[1;33m'
BLUE='\033[0;34m'
NC='\033[0m' # No Color

# Função para exibir status
show_status() {
    if [ $1 -eq 0 ]; then
        echo -e "${GREEN}✅ $2${NC}"
    else
        echo -e "${RED}❌ $2${NC}"
    fi
}

# Contador de erros
ERROR_COUNT=0

echo -e "${BLUE}🔍 Validando Frontend...${NC}"
echo "   - TypeScript compilation"
npm run typecheck
if [ $? -ne 0 ]; then
    echo -e "${RED}   ❌ Erro de compilação TypeScript${NC}"
    ERROR_COUNT=$((ERROR_COUNT + 1))
else
    echo -e "${GREEN}   ✅ TypeScript OK${NC}"
fi

echo "   - ESLint validation"
npm run lint
if [ $? -ne 0 ]; then
    echo -e "${RED}   ❌ Erro de lint${NC}"
    ERROR_COUNT=$((ERROR_COUNT + 1))
else
    echo -e "${GREEN}   ✅ ESLint OK${NC}"
fi

echo ""
echo -e "${BLUE}🔍 Validando Backend...${NC}"
echo "   - TypeScript compilation"
npm run typecheck
if [ $? -ne 0 ]; then
    echo -e "${RED}   ❌ Erro de compilação TypeScript${NC}"
    ERROR_COUNT=$((ERROR_COUNT + 1))
else
    echo -e "${GREEN}   ✅ TypeScript OK${NC}"
fi

echo "   - API validation"
# Verificar se há arquivos de API modificados
API_FILES=$(git diff --cached --name-only | grep -E "src/app/api|src/lib/validation")
if [ -n "$API_FILES" ]; then
    echo "   - Validando schemas de API..."
    npm run test:api-validation 2>/dev/null || echo -e "${YELLOW}   ⚠️  Testes de API não encontrados${NC}"
fi

echo ""
echo -e "${BLUE}🔍 Validando Database...${NC}"
echo "   - Schema validation"
npm run db:validate-schema 2>/dev/null || echo -e "${YELLOW}   ⚠️  Drizzle Kit não configurado${NC}"

echo "   - Migration validation"
# Verificar se há migrações modificadas
MIGRATION_FILES=$(git diff --cached --name-only | grep -E "drizzle/.*\.sql")
if [ -n "$MIGRATION_FILES" ]; then
    echo "   - Validando migrações..."
    for file in $MIGRATION_FILES; do
        echo "     - $file"
        # Validar sintaxe SQL básica
        if grep -q "CREATE TABLE\|ALTER TABLE\|DROP TABLE" "$file"; then
            echo -e "${GREEN}     ✅ Sintaxe SQL válida${NC}"
        else
            echo -e "${YELLOW}     ⚠️  Arquivo de migração sem comandos DDL${NC}"
        fi
    done
fi

echo ""
echo -e "${BLUE}🔍 Validando Integração Cross-Layer...${NC}"
echo "   - Schema consistency"
# Verificar consistência entre schemas
if [ -f "src/lib/validation/cross-layer.ts" ]; then
    echo "   - Validando schemas compartilhados..."
    npm run test:cross-layer 2>/dev/null || echo -e "${YELLOW}   ⚠️  Testes cross-layer não configurados${NC}"
else
    echo -e "${YELLOW}   ⚠️  Arquivo cross-layer.ts não encontrado${NC}"
fi

echo ""
echo -e "${BLUE}🔍 Validação de Segurança...${NC}"
echo "   - Verificando secrets e tokens"
# Verificar se há secrets no código
SECRET_FILES=$(git diff --cached --name-only | grep -v ".env" | xargs grep -l "password\|secret\|token\|key" 2>/dev/null || true)
if [ -n "$SECRET_FILES" ]; then
    echo -e "${YELLOW}   ⚠️  Possíveis secrets encontrados em:${NC}"
    echo "$SECRET_FILES" | while read file; do
        echo -e "${YELLOW}     - $file${NC}"
    done
fi

echo "   - Verificando dependências vulneráveis"
npm audit --audit-level=moderate 2>/dev/null || echo -e "${YELLOW}   ⚠️  npm audit não disponível${NC}"

echo ""
echo -e "${BLUE}🔍 Validação de Performance...${NC}"
echo "   - Verificando imports desnecessários"
# Verificar imports não utilizados
UNUSED_IMPORTS=$(git diff --cached --name-only | grep -E "\.(ts|tsx)$" | xargs grep -l "import.*from.*'" 2>/dev/null || true)
if [ -n "$UNUSED_IMPORTS" ]; then
    echo -e "${YELLOW}   ⚠️  Verifique imports não utilizados em:${NC}"
    echo "$UNUSED_IMPORTS" | head -5 | while read file; do
        echo -e "${YELLOW}     - $file${NC}"
    done
fi

echo ""
echo "=============================================="

# Resultado final
if [ $ERROR_COUNT -eq 0 ]; then
    echo -e "${GREEN}🎉 Validação Multi-Camada Concluída com Sucesso!${NC}"
    echo -e "${GREEN}✅ Todas as camadas foram validadas${NC}"
    echo -e "${GREEN}✅ Commit liberado para prosseguir${NC}"
    exit 0
else
    echo -e "${RED}❌ Validação Multi-Camada Falhou!${NC}"
    echo -e "${RED}❌ $ERROR_COUNT erro(s) encontrado(s)${NC}"
    echo -e "${RED}❌ Commit bloqueado - corrija os erros antes de prosseguir${NC}"
    echo ""
    echo -e "${YELLOW}💡 Dicas para correção:${NC}"
    echo "   1. Execute 'npm run validate:all' para ver detalhes"
    echo "   2. Execute 'npm run typecheck' para erros TypeScript"
    echo "   3. Execute 'npm run lint' para erros de lint"
    echo "   4. Execute 'npm run db:validate-schema' para erros de schema"
    echo ""
    exit 1
fi

